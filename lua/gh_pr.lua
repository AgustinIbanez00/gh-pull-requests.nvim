local source = require("gh-pr.neotree.source")

source.commands = source.commands or require("gh-pr.neotree.commands")
source.components = source.components or require("gh-pr.neotree.components")

return source
