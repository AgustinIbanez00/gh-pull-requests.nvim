*gh-pr.txt*    gh-pr plugin

GH-PR is a Neovim plugin to review GitHub pull requests in-editor using the
GitHub CLI as backend.

Requirements
============

- `gh` installed and authenticated
- inside a git repository

Optional UI backends:

- `neo-tree.nvim` (primary)
- `telescope.nvim` (fallback)
- `snacks.nvim` (required for interactive overview)

Commands
========

:GhPrOpen
    Opens the pull request UI. Uses Neo-tree source `gh_pr` when available,
    otherwise falls back to Telescope if enabled.

:GhPrList
    Opens Telescope query and pull request picker.

:GhPrComments [number]
    Opens the PR Review source with comments integrated for the active PR
    or provided PR number.

:GhPrStartReview [number]
    Starts review flow for active/provided PR. Prompts whether to notify
    GitHub (creates/ensures pending review) and opens PR Review source.

:GhPrReviewTree
    Toggles PR Review source (`gh_pr_review`).
    PR Review source sections: Overview, Labels, Files, Reviewers,
    Commits, Checks, Comments.
    `Comments` is rendered as tree sections: `By File` and `Global`.
    `By File` groups threads by path/file and uses status badges:
    `[UNRESOLVED]`, `[RESOLVED]`, `[CLOSED]`.
    `Global` includes review events and general PR comments.
    In `Files`, directory nodes show `X/Y VIEWED` in yellow when at least
    one descendant file is viewed (recursive count including subfolders).
    Inside `gh_pr_review`, `R` forces refresh from GitHub for active PR.
    `l` opens labels multi-select edit dialog.
    `r` opens reviewers multi-select edit dialog.
    `S` submit pending review as comment.
    `A` submit pending review as approve.
    `C` submit pending review as request changes.
    `D` discard pending review.
    `zA` expand all review nodes.
    `za` collapse all review nodes.
    `zF` expand `Files` subtree.
    `zf` collapse `Files` subtree.
    `zV` expand paths that contain viewed files.
    `zv` collapse paths that contain viewed files.
    `zC` expand `Comments > By File`.
    `zc` collapse `Comments > By File`.
    `zG` expand `Comments > Global` (including subgroups).
    `zg` collapse `Comments > Global` (including subgroups).
    `s` re-runs start-review flow for selected PR context.

:GhPrRefresh
    Refreshes PR data in active views.

:GhPrOverview
    Opens an interactive tabs overview for the active pull request.
    Requires `snacks.nvim`; if missing, an explicit error is shown.

:GhPrOverviewRefresh
    Refreshes the current overview buffer in place.

:GhPrOverviewMore {section} [count]
    Loads more items in a section (`checks`, `commits`, `timeline`).

Overview buffer keymaps:
    `a` approve review
    `d` request changes
    `c` comment review
    `m` merge PR
    `k` checkout PR branch
    `R` refresh overview
    `o` open PR URL in browser
    `C` open Comments PR tree for current PR
    `q` close buffer
    `H`/`L` previous/next tab
    `1..9` jump to tab
    `et` edit title
    `eb` edit description
    `el` edit labels (multi-select, replacement mode)
    `er` edit reviewers (multi-select, replacement mode; users + teams)
    `ea` edit assignees (comma-separated, replacement mode)
    `em` edit milestone (empty input removes milestone)
    `es` change state (`open`/`closed`)
    `ed` toggle draft status (`ready`/`draft`)
    `<CR>` open selected row action
    `<CR>` on `Commits` opens commit diff details in virtual patch buffer
    `gr` load more for current section tab
    `D` open diff for selected row (file/commit)
    `O` open original for selected file row
    `M` open modified for selected file row
    `,x` toggle PR Review source
    Every overview edit asks confirmation before execution and refreshes
    the overview on success.
    In multi-select edits (`labels`/`reviewers`), deselected current items
    are interpreted as removals.
    PR description is rendered inline as markdown in `Summary`.

:GhPrCheckout [number]
    Runs `gh pr checkout` for the active PR or provided number.

:GhPrOpenDiff
    Opens active file as virtual PR diff using configured render mode:
    vertical split, horizontal split, or unified inline.
    Image files (`png/jpg/jpeg/gif/webp/bmp/svg`) are rendered as previews
    in virtual buffers when `snacks.image` is available.
    If configured mode is `unified` for an image file, gh-pr forces split
    layout (vertical/horizontal) for base/head preview.
    If image rendering is unavailable/unsupported, fallback behavior is
    controlled by `diff_view.images.fallback_mode`:
      - `menu` opens a custom buffer with fallback actions
      - `metadata_only` renders metadata text diff in the current buffer
      - `auto_local` opens local cached image(s), then metadata fallback
      - `auto_github` opens GitHub compare URL, then metadata fallback
    If GitHub file content cannot be loaded, command fails with an explicit
    error (no implicit patch fallback buffer).
    In virtual PR buffers, `K` opens a modal floating window with comments
    for the current line.
    Press `R` to refresh current diff buffer from GitHub.
    Press `?` to open a floating shortcuts help dialog for this buffer.
    Press `q` for quick close:
      - in 2-way diff closes head/modified window and keeps base/original
      - in single-buffer views closes current buffer and opens PR Review
    Press `Q` to close current diff view(s) and open/focus PR Review.
    Additional virtual-buffer shortcuts:
      `R` refresh current diff from GitHub
      `q` quick close
      `Q` close diff view(s) and open PR Review
      `,dw` toggle whitespace diff mode (ignored/strict)
      `,dt` toggle whitespace/tab symbol rendering
      `,dm` cycle diff mode
      `,dv` force vertical split mode
      `,dh` force horizontal split mode
      `,du` force unified mode
      `gc` add inline comment at cursor (`MODIFIED`/head or `unified`)
      visual `gc` add inline comment for selected range (`v`/`V`, head or `unified`)
      image files disable `K`, `gc`, `,n`, `,p` and whitespace/layout toggles
      image files: `<CR>` runs default fallback action, `gf` opens fallback menu
      for `ADDED` files, opens single MODIFIED buffer (no split/unified layout)
      in `ADDED` single mode, `gc` is allowed on any line/range
      inline comments are validated before opening composer:
      - head: only inside diff hunks
      - unified: only on added (`+`) lines
      composer: `<C-s>` submit draft, `q`/`<Esc>` cancel
      `,n` next change
      `,p` previous change
      `,f` next file
      `,F` previous file
      `,v` next reviewed file
      `,V` previous reviewed file
      file navigation always reopens full diff view using current render mode
      (`vertical`/`horizontal`/`unified`)
      `,rs` submit pending review as comment
      `,ra` submit pending review as approve
      `,rr` submit pending review as request changes
      `,rd` discard pending review
      `,x` toggle PR Review source

:GhPrOpenOriginal
    Opens virtual readonly base version for active PR file.

:GhPrOpenModified
    Opens virtual readonly head version for active PR file.

:GhPrToggleReviewed
    Toggles local viewed state for the current PR file.

:GhPrNextChange
    Jump to next diff hunk.

:GhPrPrevChange
    Jump to previous diff hunk.

:GhPrApprove
    Prompt review message and confirm before submitting approve review.

:GhPrRequestChanges
    Prompt review message and confirm before submitting request-changes review.

:GhPrComment
    Prompt review message and confirm before submitting general comment review.

:GhPrReviewSubmit
    Submit current pending review as comment (message + confirm).

:GhPrReviewApprove
    Submit current pending review as approve (message + confirm).

:GhPrReviewRequestChanges
    Submit current pending review as request changes (message + confirm).

:GhPrReviewDiscard
    Discard current pending review (confirm).

:GhPrMerge [merge|squash|rebase]
    Merge active PR with selected strategy.

:GhPrQueryAdd
    Add query definition (folder/label/query).

:GhPrQueryEdit
    Edit existing query definition.

:GhPrQueryDelete
    Delete existing query definition.

Keymaps
=======

<leader>gho       Open PR UI (:GhPrOpen)
<leader>ghl       Open Telescope list (:GhPrList)
<leader>ghm       Open comments tree (:GhPrComments)
<leader>ghx       Toggle PR Review source (:GhPrReviewTree)
<leader>ghr       Refresh (:GhPrRefresh)
<leader>ghv       Open PR overview (:GhPrOverview)
<leader>ghc       Checkout PR (:GhPrCheckout)
<leader>ghd       Open diff (:GhPrOpenDiff)
<leader>ght       Toggle viewed (:GhPrToggleReviewed)
<leader>ghn       Next diff hunk (:GhPrNextChange)
<leader>ghp       Previous diff hunk (:GhPrPrevChange)
`C` (gh_pr)      Open comments tree (:GhPrComments)
`S` (gh_pr)      Start review flow for selected PR

Configuration
=============

Call `require("gh-pr").setup({ ... })` with:

- `remotes` (`{ "origin", "upstream" }` by default)
- `max_results` (`30`)
- `file_list_layout` (`"tree"` or `"flat"`, legacy fallback for `gh_pr`)
- `path_render.scope` (`"both"`, or `"files"` / `"comments"`)
- `path_render.mode` (`"compact"`, or `"tree"` / `"flat"`)
- `path_render.separator` (`"/"`, visual separator in compact mode)
- `path_render.show_status_prefix` (`true`, `Files` source only)
- `hide_viewed_files` (`false`)
- `line_comments.enabled` (`true`)
- `line_comments.keymap` (`"K"`)
- `line_comments.indicator_style` (`"sign_and_virtual_text"`)
- `line_comments.show_resolved` (`true`)
- `line_comments.show_outdated` (`true`)
- `line_comments.max_popup_width` (`90`)
- `line_comments.max_popup_height` (`18`)
- `line_comments.popup.enter` (`true`, modal popup on `K`)
- `line_comments.popup.position` (`"cursor"`, or `"editor"` / `"preview_window"`)
- `line_comments.popup.border` (`"rounded"`)
- `line_comments.popup.wrap` (`true`)
- `line_comments.popup.close_on_move` (`true`, only relevant for non-modal popup)
- `line_comments.popup.max_width|max_height` (`90|18`)
- `line_comments.virtual_text.enabled` (`true`)
- `line_comments.virtual_text.prefix` (`"C"`)
- `line_comments.virtual_text.show_count` (`true`)
- `line_comments.virtual_text.position` (`"eol"`, or `"inline"`)
- `line_comments.signs.open|resolved|outdated` (`"C>"`, `"C="`, `"C~"`)
- `line_comments.comments_tree.preview.keymap` (`"p"`)
- `line_comments.comments_tree.preview.position` (`"right"`)
- `line_comments.comments_tree.preview.keep_focus` (`true`)
- `overview.ui` (`"snacks"`)
- `overview.layout` (`"tabs"`)
- `overview.tabs` (`summary/checks/commits/timeline/files`)
- `overview.expand_step` (`20`)
- `overview.date_format` (`"%Y-%m-%d %H:%M"`)
- `overview.window.enabled` (`true`)
- `overview.window.border` (`"rounded"`)
- `overview.window.width_ratio|height_ratio` (`0.88`)
- `overview.window.min_width|min_height` (`100|28`)
- `overview.window.max_width|max_height` (`180|60`)
- `overview.window.backdrop` (`0`)
- `overview.window.enter` (`true`)
- `overview.theme.state_colors` (`true`)
- `overview.theme.checks_colors` (`true`)
- `overview.theme.labels` (`true`)
- `overview.theme.reviewers` (`true`)
- `overview.theme.timeline_kinds` (`true`)
- `overview.markdown.enabled` (`true`)
- `overview.markdown.provider` (`"auto"`)
- `overview.markdown.max_lines` (`500`)
- `overview.markdown.code_block_border` (`false`)
- `overview.max_items` (checks/commits/timeline)
- `overview.show` (toggle sections)
- `cache.gh_pr.enabled` (`true`)
- `cache.gh_pr.ttl_seconds` (`60`)
- `cache.gh_pr.auto_refresh_when_focused` (`true`)
- `cache.gh_pr.max_cache_age_seconds` (`900`)
- `cache.gh_pr.show_stale_badge` (`true`)
- `cache.gh_pr.sync_visible_buffers` (`true`)
- `cache.gh_pr_review.enabled` (`true`)
- `cache.gh_pr_review.ttl_seconds` (`60`)
- `cache.gh_pr_review.auto_refresh_when_focused` (`true`)
- `cache.gh_pr_review.max_cache_age_seconds` (`900`)
- `cache.gh_pr_review.show_stale_badge` (`true`)
- `cache.gh_pr_review.sync_visible_buffers` (`true`)
- `diff_view.mode` (`"vertical"`, also `"horizontal"` / `"unified"`)
- `diff_view.ignore_whitespace` (`false`)
- `diff_view.render_whitespace` (`true`)
- `diff_view.whitespace.tab` (`">-"`, tab marker in `listchars`)
- `diff_view.whitespace.space` (`"."`, space marker in `listchars`)
- `diff_view.whitespace.trail` (`"~"`, trailing space marker)
- `diff_view.whitespace.nbsp` (`"+"`, non-breaking space marker)
- `diff_view.whitespace.color` (`nil`, optional highlight color e.g. `#7f8ea3`)
- `diff_view.whitespace.highlight_group` (`"GhPrDiffWhitespace"`)
- `diff_view.shortcuts.toggle_whitespace` (`",dw"`)
- `diff_view.shortcuts.toggle_render_whitespace` (`",dt"`)
- `diff_view.shortcuts.cycle_mode` (`",dm"`)
- `diff_view.shortcuts.set_vertical` (`",dv"`)
- `diff_view.shortcuts.set_horizontal` (`",dh"`)
- `diff_view.shortcuts.set_unified` (`",du"`)
- `diff_view.images.enabled` (`true`)
- `diff_view.images.backend` (`"snacks"`, only backend currently supported)
- `diff_view.images.formats` (`png/jpg/jpeg/gif/webp/bmp/svg`)
- `diff_view.images.cache_dir` (`nil`, defaults to `stdpath("cache")/gh-pr/images`)
- `diff_view.images.fallback` (`"placeholder"`)
- `diff_view.images.fallback_mode` (`"menu"`, also `"metadata_only"` / `"auto_local"` / `"auto_github"`)
- `diff_view.images.fallback_default_action` (`"metadata"`, also `"open_local_current"` / `"open_local_both"` / `"open_github"`)
- `diff_view.images.fallback_menu_keymap` (`"gf"`)
- `diff_view.images.fallback_open_local` (`"system"`)
- `diff_view.images.fallback_github_target` (`"pr_files"`, also `"pr"`)
- `diff_view.images.show_metadata` (`true`)
- `diff_view.images.metadata_resolution_strategy` (`"hybrid"`, also `"internal"` / `"external"`)
- `diff_view.images.metadata_external_command` (`{ "magick", "identify", "-format", "%w %h", "{file}" }`)
- `diff_view.images.max_bytes` (`26214400`, 25 MiB)
- `queries` list: `{ folder, label, query }`
- `ui.use_neotree` (`true`)
- `ui.telescope_fallback` (`true`)

Persistence
===========

- Queries: `stdpath("state")/gh-pr/queries.json`
- Viewed files: `stdpath("state")/gh-pr/state.json`
- Diff view prefs (`mode`, `ignore_whitespace`, `render_whitespace`): `stdpath("state")/gh-pr/state.json`
- Image fallback default action: `stdpath("state")/gh-pr/state.json`
- PR cache: `stdpath("state")/gh-pr/pr_cache.json`
- Cache is stored per source/repo key (`gh_pr` and `gh_pr_review`).
